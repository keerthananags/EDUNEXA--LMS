import { Button } from "@chakra-ui/react";

const Hero = () => {
  return (
    <div className="flex flex-col items-center justify-center text-center min-h-[80vh] px-6 bg-gradient-to-r from-blue-500 to-indigo-600 text-white">
      
      <h1 className="text-4xl md:text-6xl font-bold mb-4">
        Welcome to EduNexa LMS
      </h1>

      <p className="text-lg md:text-xl mb-6 max-w-2xl">
        Learn anytime, anywhere. Access high-quality courses, track your progress,
        and achieve your goals with our modern learning platform.
      </p>

      <div className="flex gap-4">
        <Button colorScheme="blackAlpha" variant="solid">
          Get Started
        </Button>
        <Button colorScheme="whiteAlpha" variant="outline">
          Explore Courses
        </Button>
      </div>
      
    </div>
  );
};

export default Hero;